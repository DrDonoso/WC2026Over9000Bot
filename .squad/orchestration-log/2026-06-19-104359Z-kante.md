# Orchestration: Kanté — /tongo YAML Merge

**Timestamp:** 2026-06-19T10:43:59Z  
**Agent:** Kanté (Backend Developer, claude-sonnet-4.6)  
**Mode:** Background (one-shot)  
**Status:** SUCCESS

## Task

Consolidate `/tongo` configuration from separate `data/TongoPhrases.txt` and per-user `data/TongoUsers.yml` files into a single merged YAML with global `phrases:` section and per-user `users:` overrides.

## Deliverables

1. **`src/worldcup_bot/data/tongo.py`**
   - Removed: `load_tongo_phrases`, `read_tongo_phrase_file`, their caches
   - Added: `TongoConfig` dataclass, `load_tongo_config(path)` with mtime hot-reload

2. **`src/worldcup_bot/config.py`**
   - Removed: `tongo_phrases_path` field, `TONGO_PHRASES_PATH` env var
   - Kept: `tongo_users_path` → now points to merged file

3. **`src/worldcup_bot/bot/handlers.py`**
   - Rewrote `cmd_tongo` to use single merged config file

4. **Data templates**
   - `data/TongoUsers.template.yml` — committed, Spanish comments + 16 default phrases
   - `data/predictions.template.yml` — committed, fake user examples
   - `data/TongoUsers.yml` — gitignored runtime, pre-populated with 22 migrated phrases + commented participant usernames

5. **Tests**
   - Removed: 16 old tests (TongoPhrases, TongoPhraseFile)
   - Added: 26 new tests for TongoConfig (merged schema, hot-reload, validation)
   - Result: **1452 tests, all passing**

## Notes

- All integration tests updated to use merged YAML
- Graceful degradation: users without config fall back to 1/3 Sanchez + global pool
- No Maldini coordination needed (env var unchanged); Maldini will separately update `.gitignore` and compose files

## Reference

- Decision: `.squad/decisions.md` → "# Decision: Merge /tongo into a single YAML file" (2026-06-19)
