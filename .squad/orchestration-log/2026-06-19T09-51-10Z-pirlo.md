# Orchestration Log: Pirlo (Tech Lead)

**Session:** 2026-06-19T09:51:10Z  
**Agent:** Pirlo (claude-opus-4.5, background)  
**Task:** Design per-user `/tongo` configuration  
**Status:** SUCCESS

## Summary

Pirlo produced a comprehensive design proposal for per-user `/tongo` configuration, evaluating three options (extend `predictions.yml`, dedicated `TongoUsers.yml`, in-band syntax in `TongoPhrases.txt`). Recommended `data/TongoUsers.yml` as the cleanest approach with separation of concerns.

## Key Outputs

- **Design Document:** `.squad/decisions/inbox/pirlo-tongo-per-user.md`
- **Recommendation:** Dedicated YAML file, keyed by username (lowercase), supporting:
  - `sanchez_ratio` override (0.0–1.0, default 1/3)
  - `phrases_mode` (append or replace, default append)
  - Inline `phrases` or `phrases_file` (mutually exclusive)
- **Open decisions forwarded to DrDonoso:** File versioning (commit vs. git-ignore), inline vs. file phrases, default mode
- **Backward compatibility:** Unconfigured users must preserve exact current behavior

## Status for Implementation

Kanté ready to implement once DrDonoso confirms the open decisions (confirmed: committed file, both inline + file, append default). Implementation ~2–3 hours with tests.
