# Orchestration Log — Kanté Sprint 2026-06-17

**Date:** 2026-06-17T18:57:33Z  
**Agent:** Kanté (Backend Developer)  
**Status:** PUBLISHED on origin/main (commits 5a6e654, be7e520)

## Summary

Three robust improvements to clip finder and live match enrichment, deployed and verified live against real fixtures.

## Tasks Completed

### kante-32: Robust Clip Scorer Matching
**Commit:** 5a6e654  
**File:** `.squad/decisions/inbox/kante-clip-scorer-robust-match.md`

**Problem:** r/soccer clip posts use mixed scorer formats (e.g., "Wissa Y. goal" vs "Yoane Wissa") with accent variations. Added-time drift (±4 minutes) caused matches to fail.

**Solution:**
- Rewrite `_scorer_matches()` with accent-folding (`unicodedata.normalize` → strip combining marks).
- Tokenize and drop noise ("goal", "penalty", single chars).
- Match on token intersection + substring fallback.
- Widen minute tolerance from ±2 → ±3 (defense-in-depth; scorer match is primary).

**Tests:** 8 new scorer-match cases + Wissa scenario (minute +4, scorer carries) → **1161 tests pass**.

**Example fixes:**
| Input | Expected | Now Works |
|-------|----------|-----------|
| "Wissa Y. goal" vs "Yoane Wissa" | True | ✓ |
| "Gyökeres" vs "Viktor Gyökeres" | True | ✓ |
| "R. Leão goal" vs "Rafael Leão" | True | ✓ |

---

### kante-33: Merge HTML Search + /new/ Listing
**Commit:** 5a6e654  
**File:** `.squad/decisions/inbox/kante-clip-merge-new-listing.md`

**Problem:** r/soccer HTML search index lags real time. Very recent clips appear in `/new/` but not in search results. Previous fallback only tried `/new/` when search returned **zero** results. Cancelo goal (55') existed in `/new/` but was missed because search had returned other posts.

**Solution:**
- When JSON search fails (403), fetch **both** `_fetch_html_search_posts()` AND `_fetch_html_posts()` (live listing).
- Merge deduplicated by post id; preserve order (search first, then new entries not seen).

**Tests:** 2 new merge scenarios → **1163 tests pass**.

---

### kante-34: /endirecto Live Detail via Reddit Thread + OpenAI
**Commit:** be7e520  
**File:** `.squad/decisions/inbox/kante-endirecto-live-detail.md`

**Problem:** `/endirecto` showed only score and status. football-data.org free tier lacks `goals`, `bookings`, `substitutions` arrays for live matches.

**Solution:**
- Find r/soccer Match Thread via `RedditMatchScanner.find_match_thread()`.
- Extract "MATCH EVENTS" section and pass to new `ai/match_events.py` OpenAI extractor (temp=0.0, max_tokens=900).
- Format with new `format_live_match_detail()`: minute, score, then optional ⚽ Goles / 🟨 Tarjetas / 🔄 Cambios.
- **Graceful fallback:** any step failure → revert to score-only `format_match()`.

**Implementation:** 3 new files + updated handlers.
- `src/worldcup_bot/ai/match_events.py` — extractor + event trimmers.
- Updated `src/worldcup_bot/bot/formatters.py` — `format_live_match_detail()`.
- Updated `src/worldcup_bot/bot/handlers.py` — `cmd_en_directo` enrichment loop.

**Tests:** 42 new tests (21 match_events, 15 formatters, 6 handlers) → **1205 tests pass** (baseline 1163).

**Live verification (Coordinator):** E2E against real Portugal vs Congo DR thread rendered:
- Minute 88'
- 2 goals (with scorers)
- 3 cards (yellow/red)
- 9 substitutions
- Message 541 sent successfully to @TELEGRAM_GROUP_ID.

---

## Regression Testing

All 1205 tests pass; live /endirecto and goal-clip commands verified against real r/soccer threads. No regressions detected.

---

## Next Steps

- Monitor live goal-clip matches for edge cases (new scorer formats, unusual character sets).
- Track /endirecto enrichment success rate; if AI call failures exceed 5%, consider caching thread lookups.
