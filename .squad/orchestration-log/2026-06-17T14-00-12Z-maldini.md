# Orchestration — Maldini (2026-06-17)

## maldini-9: Remove RICH_IMAGE_CHAT_ID from Docker config

**Session:** maldini-9 (haiku, 2026-06-17 late morning)  
**Feature:** Consolidate rich-image destination to TELEGRAM_GROUP_ID

### Summary

Removed `RICH_IMAGE_CHAT_ID` env var from docker-compose.yml, docker-compose.local.yml, and .env.example. Images now send to the existing `TELEGRAM_GROUP_ID` (shared group), simplifying configuration and reducing env-var surface.

### Key Changes

1. **docker-compose.yml** (prod)
   - Removed `RICH_IMAGE_CHAT_ID: "${RICH_IMAGE_CHAT_ID:-}"` from environment.
   - Kept 4 remaining image vars: `OPENAI_IMAGE_MODEL`, `OPENAI_IMAGE_API_KEY`, `OPENAI_IMAGE_BASE_URL`, `RICH_IMAGE_HOUR`.

2. **docker-compose.local.yml** (dev)
   - Same removal as prod.

3. **.env.example**
   - Deleted `RICH_IMAGE_CHAT_ID=3041850` line and comment.
   - Updated section comment to reference `TELEGRAM_GROUP_ID`.

### Validation

✅ Both compose files validate cleanly:
```
docker compose -f docker-compose.local.yml config -q   # exit 0
docker compose -f docker-compose.yml config -q          # exit 0
```

### Rationale

- Simplifies config: one group ID instead of two.
- Reduces optional vars to document and manage.
- Aligns with Kanté's code logic (destination hardcoded to group).

### Status

✅ DONE — environment files cleaned

### Notes

- No code changes (Kanté owns config.py).
- No git commit per request.
- History logged separately.

---
