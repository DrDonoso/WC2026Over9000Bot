# Orchestration Log — Kante-4 (Backend, Investigation)

**Date:** 2026-06-27T18:49:00Z  
**Agent:** kante-4 (Backend Developer, Claude Sonnet 4.6)  
**Session Role:** Investigation  
**Status:** COMPLETE  

---

## Mission

Investigate live production symptoms:
- **A** — Missed goal notification (NZL 0-1 BEL)
- **B** — España goal announced twice (live + at/after FT)
- **C** — 4-goal catch-up (NOR 1-3 FRA)

Determine root causes and feasibility of recovery fixes.

---

## Findings

### Symptom A — Confirmed Root Cause: API Status-Flip Delay
- football-data.org takes 5–15 min to flip SCHEDULED → IN_PLAY after kickoff
- When API finally flips, it reports score already as 0-1 (goal scored during the lag)
- Bot seeds at non-zero → emits neutral catch-up, no per-goal data
- **Fix:** Seed at 0-0 at kickoff (poll_kickoff_job), so first API poll sees 0→1 as incremental delta

### Symptom C — Confirmed Root Cause: Bot Restart Mid-Match
- Bot restart clears in-memory `seen_api`, `seen_thread` to `{}`
- If `live_scores.json` didn't persist the match (new deploy or match not yet seeded), first poll seeds at final score
- Emits catch-up for all goals (e.g., 1-3 = 4 goals missed)
- **Fix:** Recover scorer+video from Reddit thread, fall back to neutral if unavailable

### Symptom B — Confirmed Open
- Three candidate root causes (B1: two separate goals perceived as duplicate; B2: save-window crash; B3: FINISHED-first-time scenario)
- No single deterministic code path identified for proper-goal double-announce in running instance
- **Action:** Owner to confirm España final score and provide logs for classification

---

## Deliverables

- **Document:** `kante-catchup-investigation-missed-goals-duplicate.md` (inbox)
- **Recommendation to Pirlo:** Implement seed-at-0-0 + thread recovery + FINISHED eviction

---

## Handoff

Passed to Pirlo for design decisions on recovery policy + FINISHED eviction.
