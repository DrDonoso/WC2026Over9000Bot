# Orchestration Log — Kante (Backend)

**Session:** 2026-07-03T07-03-08Z  
**Task:** TVE Knockout-Round Prefix + Midnight Notation Fix

---

## Summary

Diagnosed and fixed two root causes preventing La 1 TVE broadcasts from being marked in `/hoy` for knockout matches:

1. **Round-Prefix Stripping:** RTVE knockout episode names carry a leading round token (e.g., "1/16", "Semifinal"). Added `_ROUND_PREFIX_RE` regex to strip these before team parsing.
2. **Over-Midnight Notation:** La 1 uses Spanish TV convention (24:00 = midnight next day). Replaced `strptime` with manual hour/minute parsing + `timedelta` rollover in `_parse_kickoff_utc`.

## Changes

- `src/worldcup_bot/tve.py`
  - Added `_ROUND_PREFIX_RE` regex (fraction + Spanish round forms)
  - Applied round-prefix strip in `_parse_teams()`
  - Fixed `_parse_kickoff_utc()` for hours >= 24 with `timedelta` rollover
  
- `tests/test_tve.py`
  - 23 new tests (17 round prefix + 6 midnight notation)
  - Full suite: 2157 passed ✓

## Live Verification

```
tve_channel_for(ARG vs CPV):
  BEFORE: None (round-prefix blocked home_tla, La 1 had unparseable 24:00)
  AFTER:  'La 1' ✓
```

## Status

✅ Committed to main as 8087d83
