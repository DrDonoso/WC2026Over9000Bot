# Orchestration Log — Kanté

**Timestamp:** 2026-07-01T17:55:29Z  
**Role:** Backend Developer  

## Session Summary

Implemented core goal-pipeline fix: schedule-based live seeding to address ~1h free-tier football-data API lag that gated the Reddit real-time goal poller and `/endirecto`.

### Changes

1. **Schedule-Live Predicate** (`api/client.py`):
   - Added `match_is_schedule_live(match, now_utc) -> bool`
   - Returns `True` when kickoff ≤ now ≤ kickoff+4h, non-terminal status
   - Constants: `MATCH_LIVE_WINDOW` (4h), `_TERMINAL_STATUSES`

2. **Pipeline Integration**:
   - `get_live_matches()`: Include schedule-live matches (fixes `/endirecto`, standings)
   - `poll_goals_job`: Include schedule-live in `relevant` filter; seed TIMED at 0-0
   - Reddit thread matching: Added "democratic republic of the congo" alias

3. **Test Coverage**:
   - 32 new tests across test_api_client.py, test_poll_goals_job.py, test_poll_thread_goals_job.py, test_handlers.py, test_reddit_scanner.py
   - Suite: 2102 passed (+31), 0 failures

### Concurrency Guarantees

- No double-announce: both ordering traced through `reconcile()` + `goal_lock`
- No false disallowed: VAR path requires source's own score drop
- Window consistency: 4h boundary tight, no oscillation
- Null-score handling: None → 0, safely seeds at 0-0

### Committed

commit b2e9a71 — all 2102 tests passing
