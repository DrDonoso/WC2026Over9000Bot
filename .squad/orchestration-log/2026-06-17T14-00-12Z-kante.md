# Orchestration — Kanté (2026-06-17)

## kante-22: rich-image v2 — Caption + Pose

**Session:** kante-22 (sonnet, 2026-06-17 morning)  
**Feature:** Daily rich-image evolution with captions and pose variation

### Summary

Extended rich-image feature to include multimodal captions generated by the main chat model. Implemented temp-file pattern to preserve old image for before/after comparison. Consolidated image destination to `TELEGRAM_GROUP_ID` (removed `RICH_IMAGE_CHAT_ID`). Used `max_completion_tokens` for LiteLLM compatibility.

### Key Decisions

1. **Caption model:** Main chat model (multimodal), not image model key.
2. **Temp-rename:** Writes to `.new.png`, preserves old for comparison, atomically renames after caption.
3. **Destination:** `telegram_group_id` only (no separate chat ID).
4. **API param:** `max_completion_tokens` (LiteLLM doesn't accept `max_tokens`).
5. **Fallback:** Caption is best-effort; image always written even if caption fails.

### Files Changed

- `src/worldcup_bot/ai/rich_image.py` — `generate_rich_caption()`, `run_rich_iteration()` updated
- `tests/test_rich_image.py` — supporting tests

### Status

✅ LIVE — 1052 tests pass (from feature is uncommitted)

---

## kante-23: rich-image v3 — Bounded History + JSON Memo

**Session:** kante-23 (sonnet, 2026-06-17 afternoon)  
**Feature:** Rich-image history to prevent repetition, JSON caption+memo format, pose/people variation

### Summary

Added bounded plain-text history (`rich_history.txt`, 20-line cap) persisted to state volume. Updated caption generator to return JSON `{caption,memo}` tuple. Fed history to both image and caption prompts to reduce repetition. Encouraged pose/hands/people variation in image edits.

### Key Decisions

1. **History file:** `rich_history.txt`, one line per day `"{date} | nivel N | memo"`, capped at 20 lines.
2. **Caption format:** Returns JSON `{caption,memo}` parsed by caller; fallback on parse error.
3. **Context:** Person is rigging the group's porra (amañando).
4. **Dual feed:** History appended to both image and caption prompts.
5. **Composition:** Encourages free pose change, hands in view, other people in scene (identity preserved for main subject).

### Files Changed

- `src/worldcup_bot/ai/rich_image.py` — `append_history()`, `load_history_lines()`, `format_history_for_prompt()`, `generate_rich_caption()` updated
- `tests/test_rich_image.py` — +26 tests

### Test Results

✅ 1052 tests pass (+26 from kante-22)

### E2E Validation

- 5 chained iterations on chat 3041850 (msgs 501–505)
- Captions non-repeating
- `rich_history.txt` persisted and bounded
- Images allow pose/people variation

### Status

✅ IMPLEMENTED — feature uncommitted (user reviewing)

---
