# Orchestration Log: Maldini (DevOps)

**Session:** 2026-06-19T09:51:10Z  
**Agent:** Maldini (claude-haiku-4.5, background)  
**Task:** Wire TONGO_USERS_PATH environment variable  
**Status:** SUCCESS

## Summary

Maldini wired the `TONGO_USERS_PATH` environment variable into Docker infrastructure for consistency with existing patterns (`PREDICTIONS_PATH`, `TONGO_PHRASES_PATH`). No new volumes needed — `data/` already mounted.

## Files Modified

| File | Change |
|---|---|
| `docker-compose.yml` | Added `TONGO_USERS_PATH: "${TONGO_USERS_PATH:-/app/data/TongoUsers.yml}"` with comment |
| `docker-compose.local.yml` | Identical entry for parity |
| `.env.example` | Documented optional `TONGO_USERS_PATH` with purpose and default |

## Validation Completed

- ✅ **No new volumes:** `data/` already mounted read-only at `/app/data:ro`
- ✅ **Committable:** `data/TongoUsers.yml` not in `.gitignore` (git check-ignore exit code 1)
- ✅ **Parity:** Both compose files use identical entries
- ✅ **Consistency:** Matches env-var convention for `PREDICTIONS_PATH` and `TONGO_PHRASES_PATH`
- ✅ **Compose validation:** Both files validate successfully

## Design Notes

- Default path `/app/data/TongoUsers.yml` matches application-side default in `config.py`
- Pattern: `${VAR_NAME:-/app/data/filename}` mirrors existing patterns
- No code/test changes required (Kanté handles `config.py` logic)
- Supports hot-reload via mounted read-only volume
