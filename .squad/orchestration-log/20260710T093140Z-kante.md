# Kanté Orchestration — Picante Prompt Recalibration

**Timestamp:** 2026-07-10T09:31:40Z  
**Agent:** Kanté (Backend Dev)  
**Task:** Recalibrate the picante prompt conditional context usage  

## Summary

Rewrote the `_SYSTEM` prompt and inline CONTEXTO RECIENTE instruction in `src/worldcup_bot/chat/picante.py` to replace over-suppressing absolute rules ("EXCLUSIVAMENTE", "IGNÓRALOS por completo") with a balanced conditional:

- Use recent context when clearly related to the last message
- Ignore it when unrelated

**File modified:** `src/worldcup_bot/chat/picante.py` (PROMPT-ONLY, no logic changes)

**Gates/buffer/listener:** Untouched. Plumbing already passes up to `chat_buffer_size` prior messages.

**Tests:** All 156/156 green (test_chat.py + test_chat_edge_cases.py).

---

## Decision Notes

See `/decisions.md` → **Decision: Picante prompt — balanced conditional context usage**

**Key changes:**
1. REGLA DE CONTEXTO in `_SYSTEM`: Now conditional instead of absolute suppress
2. Inline label in `build_picante_user_message`: Mirrors the balanced conditional
3. Backward compatible: all gate functions and orchestration unchanged

---

**Delivered by:** Scribe (docmentation only)
