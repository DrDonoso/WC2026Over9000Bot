# Orchestration: Maldini — /tongo Infrastructure Consolidation

**Timestamp:** 2026-06-19T10:43:59Z  
**Agent:** Maldini (DevOps, claude-haiku-4.5)  
**Mode:** Background (one-shot)  
**Status:** SUCCESS

## Task

Update infrastructure files (`.gitignore`, `docker-compose.yml`, `docker-compose.local.yml`, `.env.example`) to reflect Kanté's consolidated `/tongo` YAML merge. Remove `TONGO_PHRASES_PATH` env var; add `data/TongoUsers.yml` to gitignore.

## Changes

### `.gitignore`
- Added: `data/TongoUsers.yml` (runtime file, with comment)
- Placed after `data/predictions.yml` for logical grouping

### `docker-compose.yml` (prod)
- Removed: `TONGO_PHRASES_PATH` env-var block
- Updated: `TONGO_USERS_PATH` comment to reflect merged structure

### `docker-compose.local.yml` (dev)
- Removed: `TONGO_PHRASES_PATH` env-var block (identical to prod)
- Updated: `TONGO_USERS_PATH` comment

### `.env.example`
- Removed: Commented example for `TONGO_PHRASES_PATH`
- Updated: Commented example for `TONGO_USERS_PATH` to document merged schema

## Validation

✅ `docker compose config -q` → exit 0 (both prod and local)

## Rationale

- Single image everywhere: same image, different env vars per environment
- Runtime file protection: `data/TongoUsers.yml` gitignored; users copy template and edit locally
- Backward compatibility: old `TONGO_PHRASES_PATH` silently ignored by Docker Compose

## Reference

- Decision: `.squad/decisions.md` → "# Decision: /tongo YAML Merge — Infrastructure Consolidation" (2026-06-19)
